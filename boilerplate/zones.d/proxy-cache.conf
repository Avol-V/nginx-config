# Proxy cache zones below
# At some point you'd probably want to change these paths to their own
# directory, for example to /var/cache/nginx/
proxy_cache_path /var/cache/nginx/proxy/ levels=1:2 keys_zone=proxyCache:16m max_size=1g inactive=6h use_temp_path=off;
